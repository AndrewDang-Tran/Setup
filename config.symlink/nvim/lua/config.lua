vim.diagnostic.config({
  virtual_text = true,  -- This shows the error message inline
  signs = true,         -- This shows symbols in the gutter
  underline = true,
  update_in_insert = false,
  severity_sort = true,
})
